# Python cache & build artifacts
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
*.egg
dist/
build/

# Lock files & minified assets
*.lock
# The build-dependency lock IS meant to be committed — it pins the
# hash-verified toolchain for reproducible signed release builds.
!requirements-build.lock
*.min.js

# Testing & coverage
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# Type checking
.mypy_cache/

# Virtual environments
.venv/
venv/
env/

# Distribution / packaging
SDIST/
wheels/
*.whl
*.tar.gz

# Environment / secrets
.env
.env.*

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Man page compiled output (keep source, not groff output)
*.gz

# Temporary ffmpeg working files that might escape the temp dir
ffmetadata.txt
concat.txt

# Website / marketing folder
m4Bookmaker/
devlog/

# Ho System process docs (private; versioned in nested repo)
ho-process/
