# Docker Usage Guide

> This document is a stub. Full content will be added in Phase 4.

## System Requirements

- `ffmpeg >= 6.0`
- `ffprobe >= 6.0` (bundled with ffmpeg)
- Python >= 3.11

## Docker Setup

_Sample Dockerfile, `docker run` examples, and volume mount instructions will be added in Phase 4._
